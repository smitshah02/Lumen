# Lumen — Python dependencies
#
# Python 3.14 (dev env: 3.14.5). Versions below are pinned to the working
# environment this project's published results were produced on.
#
#   python3.14 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#
# Not installed by pip:
#   - Postgres 16 + pgvector  -> docker-compose.yml
#   - Ollama + qwen2.5:14b    -> `ollama serve && ollama pull qwen2.5:14b`
#   - Model weights in models/ -> MedCPT query/article/cross-encoder, BGE reranker

# --- Database -------------------------------------------------------------
SQLAlchemy==2.0.49
psycopg2-binary==2.9.12

# --- Embeddings & rerankers (MedCPT, BGE) ---------------------------------
# torch: MPS on Apple Silicon, CPU fallback elsewhere.
torch==2.11.0
transformers==5.8.0
numpy==2.4.4

# --- De-identification (Presidio + spaCy) ---------------------------------
presidio-analyzer==2.2.359
presidio-anonymizer==2.2.362
spacy==3.8.13
# src/deid/pipeline.py loads en_core_web_lg by name; it is not on PyPI.
# Installed here directly so `pip install -r requirements.txt` is one command.
# Equivalent: python -m spacy download en_core_web_lg
https://github.com/explosion/spacy-models/releases/download/en_core_web_lg-3.8.0/en_core_web_lg-3.8.0-py3-none-any.whl

# --- Guideline ingestion ---------------------------------------------------
pypdf==6.11.0

# --- Ollama client (judge + answer generation talk to it over plain HTTP) --
requests==2.33.1

# --- Ingestion / utilities -------------------------------------------------
pandas==3.0.2
tqdm==4.67.3
python-dotenv==1.2.2

# --- Optional: reranker training-data tooling ------------------------------
# Only src/reranker/audit_training_quality.py needs this. It points the OpenAI
# client at Groq's OpenAI-compatible endpoint and requires GROQ_API_KEY.
# It sends *training-data* text off-machine — do not run it on MIMIC note text
# you are holding under the PhysioNet DUA.
# openai==2.36.0
