# Lumen — Python dependencies
#
# Canonical runtime: CPython 3.12. Versions below are pinned direct
# dependencies from the validated environment; see docs/runtime-compatibility.md.
#
#   python3.12 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#
# Not installed by pip:
#   - Postgres 16 + pgvector  -> docker-compose.yml
#   - Ollama + two model tiers -> `ollama serve && ollama pull <tag>`      (runtime; LUMEN_LLM_MAIN/FAST)
#   - qwen2.5:14b             -> retrieval-eval judge only (src/evals/ollama_backend.py)
#   - Model weights in models/ -> runtime MedCPT query/article + BGE; the
#     MedCPT cross-encoder is a legacy-eval-only profile

# --- Database -------------------------------------------------------------
SQLAlchemy==2.0.49
psycopg2-binary==2.9.12

# --- Embeddings & rerankers (MedCPT, BGE) ---------------------------------
# torch: MPS on Apple Silicon, CPU fallback elsewhere.
torch==2.11.0
transformers==5.8.0
huggingface-hub==1.14.0
numpy==2.4.4

# --- De-identification (Presidio + spaCy) ---------------------------------
presidio-analyzer==2.2.359
presidio-anonymizer==2.2.362
spacy==3.8.13
# src/deid/pipeline.py loads en_core_web_lg by name; it is not on PyPI.
# Installed here directly so `pip install -r requirements.txt` is one command.
# Equivalent: python -m spacy download en_core_web_lg
https://github.com/explosion/spacy-models/releases/download/en_core_web_lg-3.8.0/en_core_web_lg-3.8.0-py3-none-any.whl

# --- Guideline ingestion ---------------------------------------------------
pypdf==6.11.0

# --- Ollama client (judge + answer generation talk to it over plain HTTP) --
requests==2.33.1

# --- Agent graph + Postgres checkpointer (src/agents/graph.py) -------------
langgraph==1.1.10
langgraph-checkpoint-postgres==3.1.2
# psycopg 3 + pool: PostgresSaver's driver. -binary bundles libpq so no system
# package is needed. (psycopg2-binary above is SQLAlchemy's driver; both stay.)
psycopg==3.3.4
psycopg-binary==3.3.4
psycopg-pool==3.3.1

# --- MCP server (src/mcp_server/) -------------------------------------------
mcp==2.1.1

# --- HTTP API (src/api/) -----------------------------------------------------
fastapi==0.136.1
uvicorn==0.46.0
pydantic==2.13.4
typing-extensions==4.15.0

# --- Ingestion / utilities -------------------------------------------------
pandas==3.0.2
tqdm==4.67.3
python-dotenv==1.2.2

# --- Optional: tracing (src/obs/tracing.py) --------------------------------
# Only needed when LUMEN_TRACING=1. Traces carry note text in span inputs, so
# research tracing must stay local; demo tracing may use remote HTTPS.
# src/obs/tracing.py enforces the selected plane's policy.
langfuse==4.15.1
langchain==1.2.18
